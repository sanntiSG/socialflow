[build]
  base = "frontend"
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Variables de entorno a configurar en el panel de Netlify:
# REACT_APP_API_URL = https://tu-api.onrender.com
# REACT_APP_NAME = SocialFlow
